# blended — harness design decisions and their evidence

Each decision below names the paper it rests on (DOI, so any agent can
re-look it up through home-still: `catalog_read` / `paper_get`), the
measured result that motivates it, and the code that implements it.
The corpus inventory behind these is `docs/research/` (08-20 evidence
brief, 08-22 corpus catalog, 08-25 skills vetting).

| # | Decision | Evidence (DOI) | Code |
|---|----------|----------------|------|
| 1 | The agent talks to a purpose-built tool surface (`run_python` + gate report, `inspect_object`, `render_views`, `inspect_domain`, bounded `list_scene`), never a bare shell. | SWE-agent: an agent-computer interface beat a bare shell 18.0% vs 11.0% on the same model — `10.48550/arXiv.2405.15793` | `src/blended/agent/tools.py` (`TOOL_SCHEMAS`, `dispatch_tool`), `src/blended/manifest.py` |
| 2 | Every build step is self-verified by a hard, deterministic gate before any visual critique is spent; the domain lanes get their own verifier (`inspect_domain`); and the gate asserts what the user can OBSERVE and USE, not only what the mesh measures — an object that is unlinked, in an excluded collection, hidden, `hide_render`, or carrying a collapsed / non-finite object transform fails with the cause named, because flawless geometry nobody can see (or whose world-space numbers are NaN) is not a pass. | Voyager ablation: removing self-verification costs −73% of discovered items, the largest of six components — `10.48550/arXiv.2305.16291` | `src/blended/harness.py` (`run_chunk`, `scene_state_failure`, `invisibility_failure`, `degenerate_transform_failure`), `src/blended/analyze/mesh_checks.py`, `inspect_object` / `inspect_domain` in `tools.py`, prompt v11 "Beyond the mesh" |
| 3 | Two-stage critique: geometry gate first, VLM eye second, and the eye *describes*, it does not adjudicate. | BlenderGym: best VLM-verifier/human alignment 0.66 vs 0.79 inter-human, with fixed position bias in some models — `10.48550/arXiv.2504.01786` | `VISION_DESCRIBE_PROMPT`, `VisionDescriber` in `src/blended/agent/loop.py`; `src/blended/evaluate/examiner.py` |
| 4 | Reference-guided examination: the golden render is sent alongside the candidate in the same call. | RESP: reference guidance is worth +0.32 F1 over an isolated frame, fixing recall collapse (0.28 → 0.76) — `10.48550/arXiv.2604.11082` | `src/blended/evaluate/examiner.py`, `_evaluate/golden/` |
| 5 | Edits operate on the existing scene/script, never a rebuild from scratch; the refinement gate measures that untouched dimensions stay the SAME. | LL3M §3.2 / Fig. 17: feeding prior code is what makes edits local; rewriting from scratch yields a different asset — `10.48550/arXiv.2508.08228` | prompt "Changing something they already have" (`prompts/working_agreement_v10.md.j2`), `src/blended/evaluate/acceptance.py`, `chat_e2e.py::check_iterative` |
| 6 | API drift is caught by an error-driven catalog that attaches the known fix to the traceback the model reads. | LL3M's BlenderRAG retrieval is error-driven (e.g. `Specular` → `Specular IOR Level`) — `10.48550/arXiv.2508.08228`; LLMs cannot self-correct reasoning without external signal — `10.48550/arXiv.2310.01798` | `src/blended/drift/catalog.py`, `run_source_in_process` in `src/blended/run/executor.py` |
| 7 | Mistake memory is procedural: failure → cause → fix → guarding assertion, consulted before any adjustment. | CoALA: reflect on experience into semantic knowledge, then procedural knowledge as a code library — `10.48550/arXiv.2309.02427`; memory-mechanism survey on cross-trial memory and the stale-memory problem — `10.48550/arXiv.2404.13501` | `src/blended/evaluate/mistake_memory.py` (`MistakeRecord`, `consult`) |
| 8 | The system prompt is a versioned artifact: one contiguous hunk per revision, hypothesis stated before the run, outcome recorded after, pinned only on converged evidence. | ProTeGi textual gradients (describe the flaw, generate fixes, select) — `10.18653/v1/2023.emnlp-main.494`; The Prompt Report's finding that prompts are sensitive to detail without obvious reason, so convergence needs repeated clean runs — `10.48550/arXiv.2406.06608` | `src/blended/agent/prompt_versions.py` (`changed_hunks`, `validate_revisions`, `PINNED_PROMPT_REVISION`), `src/blended/agent/prompt_search.py` |
| 9 | The gate's check list is the designed-mesh defect taxonomy: non-manifold edges, gaps, self-intersections, inconsistent normals. | Polygon Mesh Repairing survey — `10.1145/2431211.2431214`; metric framework separating artifact validity from quality — `10.1109/TPAMI.2024.3398998` | `src/blended/analyze/mesh_checks.py`, `MeshBudget` |
| 10 | The user can stop the agent at any step, and the stop leaves the conversation consistent; the reply streams so the user sees what the agent is doing while it works. | Guidelines for Human-AI Interaction G8 "support efficient dismissal", G9 "support efficient correction", G11 "make clear why the system did what it did" — `10.1145/3290605.3300233` | `AgentSession.cancel`, `_cancel_turn`, `stream_replies`, `_assemble_openai_stream` / `_assemble_ollama_stream` in `src/blended/agent/loop.py`; `BLENDED_OT_stop`, live bubble in `blender_addon/__init__.py` |
| 11 | The user never writes prompts: they say what they want in plain words, and the harness supplies the working agreement, the op manifest, and the verification discipline. | Why Johnny Can't Prompt: non-experts design prompts opportunistically and prompt-based interactions are brittle — `10.1145/3544548.3581388` | `src/blended/agent/system_prompt.py` (`build_system_prompt`), `blender_addon/__init__.py` composer (one text box, no prompt fields) |
| 12 | The ops vocabulary is context-free data-API construction (bmesh, datablocks), not `bpy.ops` primitives; where an operator is unavoidable (heat-map skinning) it is wrapped once with a documented `temp_override`. | CodeAct: executable code as the action space, with environment feedback — `10.48550/arXiv.2402.01030`; BlenderLLM on API drift across versions — `10.48550/arXiv.2412.14203` | `src/blended/ops/*` (`primitives.py` convention, `rigging.py::bind_mesh_to_armature`) |
| 13 | Multi-image messages to a local VLM carry explicit, text-separated `[img]` placeholders so same-size images are not fused into video frames. | Measured in-house (see `_image_placeholders` docstring; ollama/ollama#17321, ggml-org/llama.cpp#24303) against the RESP pairing requirement — `10.48550/arXiv.2604.11082` | `_image_placeholders` in `src/blended/agent/loop.py` |
| 14 | The external quality signal is a frozen 3DCodeBench subset scored by the bench's own unmodified scorers; chamfer deltas below per-roll noise are not treated as signal. | 3DCodeBench — `10.48550/arXiv.2606.01057` | `scripts/run_3dcode_instance.py`, `scripts/sweep_3dcode.py`, `scripts/diagnose_3dcode.py`, `bench_sets/` |
| 15 | On a transport with no tool-calling field of its own (the headless Claude Code CLI), tool calls are CONSTRAINED by a JSON schema built from `TOOL_SCHEMAS` — one `oneOf` variant per tool, `name` pinned by `const`, `arguments` reusing that tool's own parameter schema — instead of parsed out of prose. The model's built-in tools are switched off, so the harness's six tools remain the only interface. | Grammar-constrained decoding guarantees the output conforms to the given structure without finetuning, for models that otherwise "struggle with reliably generating complex output structures" — `10.18653/v1/2023.emnlp-main.674` | `envelope_schema`, `assistant_message_from_envelope`, `ClaudeCodeTransport.command` (`--json-schema`, `--tools ""`) in `src/blended/agent/claude_code.py`; `tests/pure/test_claude_code_lane.py` |
| 16 | A turn that changes the scene declares a numbered, user-readable plan FIRST; the plan is shown with Blender's own progress bar over its steps and the user can interrupt at any step. It is a shared representation, not an approval gate — no click stands between the plan and execution. | Magentic-UI: a plan as a sequence of natural-language steps shared between user and agent, edited in place, with a progress bar over the steps during execution — `10.48550/arXiv.2507.22358`; the reason it is not a gate: users approve ≈93 % of permission prompts, so a confirmation buys oversight it does not deliver — `10.48550/arxiv.2604.14228`; planner/actor/critic with the human as supervisor measurably improves geometric accuracy and completion on Blender tasks — `10.48550/arxiv.2601.05016` | `src/blended/agent/plan.py` (`TurnPlan`, `parse_plan_arguments`, `MISSING_PLAN_REFUSAL`), `declare_plan` + `plan_step` in `src/blended/agent/tools.py`, `require_plan` enforcement and `plan`/`step` events in `AgentSession.send`, `BLENDED_PT_chat._draw_plan` |
| 17 | Every prompt carries the live selection: active object, selected objects with their world dimensions, mode, object count, frame. "Make this taller" is the natural way to ask, and the harness answers it with Blender's own selection rather than a widget. | Survey of UI design and interaction techniques in generative AI: selection (single, multi, lasso) is the second-largest family of user-guided controllability techniques, and a conversational UI that only has a prompt box uses one technique out of ~14 — `10.48550/arXiv.2410.22370` | `src/blended/agent/scene_context.py` (`collect_scene_context`, `render_scene_context`, `summarize_scene_context`), `BLENDED_OT_send.execute` |
| 18 | The render the agent judged is shown in the panel BESIDE the previous one, labelled before/now, each with a one-click enlarge. | RESP: pairing a candidate with a reference is worth +0.32 F1 / +0.12 accuracy for visual defect detection, almost all of it recovering recall (0.28 → 0.76) — `10.48550/arXiv.2604.11082` | `src/blended/ui/previews.py` (`RenderPreviews`, `paired_render_paths`), `render` event in `AgentSession.send`, `BLENDED_PT_chat._draw_evidence` / `_draw_thumbnail`, `BLENDED_OT_show_render` |
| 19 | A turn is exactly one undo step: a named restore point is pushed before it starts and Blender's per-operator pushes are suppressed for its duration, so one Ctrl+Z — or the panel's Revert turn — removes the whole turn. Cheap reversal is what licenses decision 16's missing gate. | Heuristics for AI-driven graphical asset generation tools, H-LAN: match the design language of the host environment, "the ability to undo and redo changes" included, "through appropriate use of front-end UI APIs within the host software"; practitioners also accept generation times up to 10 min, so the cost to optimise is review, not latency — `10.1080/10447318.2026.2632170` | `src/blended/ui/turn_undo.py` (`TurnUndoGuard`, `revert_turn`), `BLENDED_OT_send.execute`, `_drain_tool_requests`, `BLENDED_OT_revert_turn` |
| 20 | Each traffic event (tool call, result, thinking, vision, scene context, render) is its own collapsed `layout.panel()`: the header line says what happened, the body is one click away, and the answer is never pushed off-screen. Prose is opt-in per item rather than by one global switch. | Ecological Interface Design: do not force processing to a higher cognitive level than the task demands, while still supporting all three levels — `10.1109/21.156574` | `BLENDED_PT_chat._draw_traffic` / `_draw_body`, `_COMPACT_KINDS`, `_EVENT_PANEL_IDNAME` in `blender_addon/__init__.py`; `tests/blender/test_chat_panel_heuristics.py` |
| 21 | Keyboard bindings are printed on the controls that carry them (`Send ⌘⏎`, `⌃↑ recalls` in the composer placeholder). | ExposeHK: showing hotkeys in place "substantially improve[s] the user's transition from a 'beginner mode' of interaction to a higher level of expertise" — `10.1145/2470654.2470735` | `BLENDED_PT_chat.draw` (send row, composer placeholder), `_register_keymaps` in `blender_addon/__init__.py` |
| 22 | The harness never volunteers work: no proactive suggestions, no auto-continuation. If that ever changes, the trigger is a natural workflow boundary and stays user-controllable. | Five-day in-IDE field study of proactive AI, 18 developers, 5,732 interaction points: suggestions at natural workflow boundaries (especially after commits) were significantly more likely to be accepted, SUS 72.8 [64.1, 81.5], and participants demanded control over triggering — `10.1145/3742413.3789148` | Absence is the decision: `AgentSession.send` runs only from `BLENDED_OT_send`; recorded in `docs/2026-09-05-chat-ux-literature-review.md` §6 |
| 23 | Division of labour is explicit: the user owns intent and taste, the agent owns routine construction, and the UI is scored by how cheaply the user can verify — not by how much the agent does per turn. | Meta-analysis of 106 experiments / 370 effect sizes: decision tasks lose, CREATION tasks gain, and synergy appears when the human alone outperforms the AI alone (g = 0.46 [0.28, 0.66]) — `10.1038/s41562-024-02024-1`; experienced developers were measurably slowed on familiar large repositories while believing they were faster — `10.48550/arXiv.2507.09089`; uncertainty stated impersonally reduces overreliance without the trust penalty of first-person hedging — `10.48550/arxiv.2405.00623` | `docs/2026-09-05-chat-ux-literature-review.md`, `docs/2026-09-05-chat-ux-overhaul-plan.md`, gate verdicts surfaced verbatim in `run_python` results (`src/blended/agent/tools.py`) |
| 24 | The chat is TWO panels: a pinned working surface (status, plan, renders, the newest answer, the prompt box) and a `DEFAULT_CLOSED` record panel below it. The surface is a BOUNDED number of rows — the answer is capped by what the region has left, the finished plan collapses to its progress bar — because a Blender region cannot be scrolled from code, so anything that grows with the conversation puts the prompt box out of reach. Measured: a real sidebar is 561 × 1104 px at ui_scale 2.0, which is **27 rows**. | The conversational UI's own division of space — the prompt box is the primary interaction space, the history the secondary one — `10.48550/arXiv.2410.22370`; review cost is the thing that decides whether the agent helped, so the answer may not be behind a scroll — `10.48550/arXiv.2507.09089` | `_ChatDrawing`, `BLENDED_PT_chat` (`bl_order = 0`), `BLENDED_PT_history` (`bl_order = 1`, `DEFAULT_CLOSED`), `_answer_row_budget`, `_draw_plan(steps=…)` in `blender_addon/__init__.py`; `tests/blender/test_chat_panel_heuristics.py` |
| 25 | The shipped eye is the WRITER's own model (`claude-code:sonnet`), so a render reaches the writer as a native image block instead of a prose summary — `uses_separate_eye` is False and no second model call happens. The eye slot is earned by CALIBRATION, never by assumption: measured on the fixture zoo 2026-09-05 at sensitivity 0.80 (4/5) and control specificity 1.00 (5/5), `problems()` empty. Its miss is complementary to the superseded kimi eye's (kimi missed `lid_offset` and saw `floating_seat`; this one is the reverse). | RESP: pairing the candidate with a reference lifts recall from 0.28 (no reference) to 0.76 (oracle reference), +0.32 F1 — and a WRONG reference is worse than none at −0.23 F1, which is why the licence is identity-bound — `10.48550/arXiv.2604.11082`; one false alarm on a clean control ends refinement early, so control specificity must be exactly 1.0 — `10.48550/arXiv.2606.15693` | `RECOMMENDED_MODELS["eye"]` in `src/blended/agent/loop.py`, `CONVERGENCE_VISION_MODEL` in `prompt_versions.py`, `_evaluate/eye_calibration.json`; `tests/pure/test_examiner.py::test_the_shipped_eye_holds_the_licence_in_the_repository` |
| 26 | The shipped WRITER is qualified against the run log, not against a constant: `claude-code:sonnet` passed the structural, form and refinement gates on 5/5 briefs at the pinned prompt, first attempt (iterations 52-56, 2026-09-05), each run declaring its plan before its first edit. `CONVERGENCE_WRITER_MODEL` keeps naming `deepseek-v4-pro:cloud` because that writer's runs MINTED the golden renders — so the visual gate reports drift for any other writer by construction, and re-minting is reserved for a human. The qualification sweep lives in its own artifact, because the convergence rule reads TRAILING cycles and an appended sweep would displace the pin's own evidence. | Tool feedback outranks model feedback, so the deterministic gate is the qualifying signal and the reference-based verdict is not — `10.48550/arXiv.2409.02977`; a wrong reference is worse than none — `10.48550/arXiv.2604.11082` | `RECOMMENDED_MODELS["writer"]`, `_evaluate/writer_qualification_iterations.jsonl`; `tests/pure/test_prompt_templates.py::test_the_shipped_configuration_is_the_converged_configuration`, `tests/blender/test_addon_registration.py::test_the_addon_and_the_library_ship_the_same_models` |
| 27 | An asset whose parts cannot be TOLD APART in a render has not delivered its brief, and that is a numeric probe (`DistinctMaterialSpec`), not an eye's opinion. The crate's lid has the body's exact footprint and sits flush on the rim, so colour is the only thing that makes it visible: the parts must differ by ≥ 0.10 in linear-RGB base colour. Contrast, never a required hue — pinning a colour would bake one writer's taste into the spec. | Verifier-human alignment is 0.66 against 0.79 inter-human, so a judgement the geometry layer can make must not be delegated to the eye — `10.48550/arXiv.2504.01786`; tool feedback outranks model feedback — `10.48550/arXiv.2409.02977` | `DistinctMaterialSpec` + `CRATE_LID_MINIMUM_COLOUR_DISTANCE_RGB` in `src/blended/evaluate/briefs.py`, `_base_colour` in `acceptance.py`; `tests/blender/test_acceptance_gate.py::test_a_lid_the_same_colour_as_the_body_fails_even_though_it_measures_right` |
| 28 | Two references, because two instruments ask different questions. The VISUAL GATE asks "did the render move from the accepted state?", so it compares against the PINNED revision. The EXAMINER asks "does this deliver the brief?", so it compares against an exemplar of the configuration UNDER TEST. Pointing the examiner at the pinned reference made it answer a third question — "does this look like what another writer built?" — and returned every incidental choice of that writer as a deviation. | A wrong or irrelevant reference is worse than none at all (−0.23 F1), so which reference an instrument holds is a correctness question — `10.48550/arXiv.2604.11082` | `pinned_golden_directory` / `examiner_golden_directory` in `scripts/run_agent_task.py`; `clean_cycle_for_identity` in `src/blended/evaluate/iteration_log.py` |



## How to extend this table

A new design decision earns a row only with a DOI that can be resolved
through home-still (`paper_search` → `paper_download` → `catalog_read`
for the citation) and a code path that a reader can open. The same DOI
goes in a comment at that code path (CLAUDE.md rule), so the two stay
findable from either direction.
