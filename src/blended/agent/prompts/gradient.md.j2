You are improving the WORKING AGREEMENT given to an agent that builds 3D
game assets in Blender by writing Python and calling tools.

Here is the working agreement that ran:

--- BEGIN WORKING AGREEMENT ---
{{ body }}
--- END WORKING AGREEMENT ---

Here is what the DETERMINISTIC GATES measured on runs that used it. These
are numbers from executable checks against the built geometry, not
opinions about the renders:

--- BEGIN MEASURED FAILURES ---
{{ evidence }}
--- END MEASURED FAILURES ---

State, in at most five bullets, what about the working agreement's TEXT
allowed these measured failures. Point at specific wording: a
missing instruction, an ambiguous phrase, an ordering that lets the agent
stop too early, a requirement stated but never made checkable.

Do NOT propose fixes. Do NOT restate the failures. Do not speculate about
the model, the tools or the harness — only about what this text says and
fails to say.
